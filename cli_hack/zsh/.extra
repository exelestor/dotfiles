LS_COLORS=$LS_COLORS:'di=0;34:' ; export LS_COLORS
#cd ~/stuff
alias cds='cd ~/stuff'

export PATH=$PATH:~/.scripts

export GOPATH="/home/nb-karpochev/stuff/go"
export GOBIN=$GOPATH/bin

[[ -z "$TMUX" ]] && ( tmux attach || tmux )
